package com.jafpl.steps

/** A data provider.
  *
  * There are two forms of provider, [[com.jafpl.steps.DataProvider]] and [[com.jafpl.steps.StepDataProvider]].
  * This trait is a common ancestor for them both.
  *
  */
trait Provider {
}
